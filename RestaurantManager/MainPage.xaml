<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:RestaurantManager.Controls;assembly=RestaurantManager"
             x:Class="RestaurantManager.MainPage">

    <StackLayout Spacing="0">
        <Frame BackgroundColor="#2196F3" Padding="20" CornerRadius="0">
            <Label Text="Welcome to Restaurant Manager" HorizontalTextAlignment="Center" TextColor="White" FontSize="25"/>
        </Frame>
        <Grid Background="Black" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <controls:GridButton Grid.Row="0" Grid.Column="0" Text="Todo's"/>
            <controls:GridButton Grid.Row="0" Grid.Column="1" Text="Reservations"/>
            <controls:GridButton Grid.Row="1" Grid.Column="0" Text="Shifts"/>
            <controls:GridButton Grid.Row="1" Grid.Column="1" Text="Settings"/>
        </Grid>
    </StackLayout>

</ContentPage>
